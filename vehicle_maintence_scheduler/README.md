# Vehicle Scheduling

## Run

```bash
export AFFORDMED_ACCESS_TOKEN='token'
npm run run
```

The result is saved in `output.json`.

## Server

```text
GET  /health
GET  /depots
GET  /vehicles
POST /schedule
```

## Direct run

```bash
npm run run
```
